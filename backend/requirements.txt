# Exact pins (tested set). Reproducible builds; bump deliberately.
#
# The torch index URL is NOT set here — the Dockerfile passes it via build arg
# (TORCH_INDEX_URL). For local pip installs outside Docker, set the env var or
# pass --extra-index-url manually.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic-settings==2.15.0
numpy==2.5.2
pillow==12.3.0
# >=1.12.2 required: GHSA-24p2-j2jr-386w / CVE-2026-27809 (compression handling)
psd-tools==1.18.0
open_clip_torch==3.3.0
torch==2.13.0
# Never imported directly, but a hard transitive dependency of open_clip_torch.
torchvision==0.28.0
faiss-cpu==1.15.0
slowapi==0.1.9
limits>=3.14.0
# Auth dependencies
argon2-cffi==25.1.0
PyJWT==2.13.0
email-validator==2.3.0
