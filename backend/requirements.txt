# Exact pins (tested set). Reproducible builds; bump deliberately.
#
# CPU-torch note: the --extra-index-url below is ADDITIVE to PyPI. Pinning
# torch to an exact version makes pip compare candidates across both indexes;
# per PEP 440 local-version ordering the cpu index's `X.Y.Z+cpu` build sorts
# HIGHER than PyPI's plain `X.Y.Z`, so the slim CPU wheel wins on Linux
# instead of the multi-GB CUDA stack. (macOS wheels only exist on PyPI.)
--extra-index-url https://download.pytorch.org/whl/cpu
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic-settings==2.15.0
numpy==2.5.2
pillow==12.3.0
# >=1.12.2 required: GHSA-24p2-j2jr-386w / CVE-2026-27809 (compression handling)
psd-tools==1.18.0
open_clip_torch==3.3.0
torch==2.13.0
torchvision==0.28.0
faiss-cpu==1.15.0
